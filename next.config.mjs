// =====================================================================
// CONFIGURACIÓN DE NEXT.JS
// ---------------------------------------------------------------------
// Sin opciones especiales: el build por defecto (Turbopack) y el modo
// de despliegue se gestionan desde Vercel o Docker.
// =====================================================================

/** @type {import("next").NextConfig} */
const nextConfig = {};

export default nextConfig;
